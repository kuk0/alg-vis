package algvis.visual;

public class FixedPos extends Position { // fixny bod so suradnicami (x,y)
	public FixedPos(float x, float y) {
		this.x = tx = x;
		this.y = ty = y;
	}

	public void update() {
	}
}
